import styled from "styled-components";
export const ItemContainer = styled.div`
    widht: 80%;

    h3{
        font-size: 32px;
        color: #fafafa;
    }

    p{ 
        font-size: 16px;
        color: #fafafa60;
        margin-bottom: 20px;
    }

    a{
        color: #8080ff;
        margin-top: 20px;
        cursor: pointer;
    }

    .remover {
        background: none;
        color: #ff0f0f;
        border: none;
        padding: 0;
        font: inherit;
        cursor: pointer;
        text-decoration: underline;
      }

    hr{
        color: #fafafa60;
        margin: 20px 0;
    }

`